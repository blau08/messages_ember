{{outlet}}

<div class="container">
  
<h2>More information about {{model.name}}'s {{model.question}}</h2>

{{question-detail question=model}}

{{update-question question=question update="update"}}
<button {{action 'delete' question}}>Delete</button>
</div>
